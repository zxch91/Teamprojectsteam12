import React from "react";
import Router from "next/router";
import { Button, TextField, Typographys, Text} from '@mui/material';

const ChatPage = () => {
     return (
      <div >
        <Button>Hello</Button>
      </div>
      
    );
  };
  
  export default ChatPage;   
